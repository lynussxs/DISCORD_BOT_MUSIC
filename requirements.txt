# Discord bot framework — voice extra pulls in PyNaCl + davey for encryption
discord.py[voice]>=2.3.2

# davey is discord.py's low-level voice helper (installed automatically with [voice])
# Listed explicitly so `pip install -r requirements.txt` always gets it
davey>=0.1.5

# PyNaCl — cryptographic library required for voice connections
PyNaCl>=1.5.0

# YouTube audio extraction and streaming
# KHÔNG ghim version cứng (dùng >=) — cố ý để luôn lấy bản MỚI NHẤT mỗi lần
# build, vì YouTube liên tục đổi cách trả stream (SABR/PO-Token) và yt-dlp vá
# theo gần như hàng tuần — bản cũ dễ bị "Requested format is not available".
yt-dlp>=2026.7.4

# --- Có thể KHÔNG còn dùng nữa — kiểm tra bằng:
#   grep -rn "youtube_search\|import requests\|import anthropic" --include="*.py" .
# Nếu không có import nào khác ngoài chính dòng khai báo package → xoá được.
youtube-search-python
requests
anthropic
# ---

spotipy
httpx

# Loads .env files into os.environ
python-dotenv>=1.0.0

# Lightweight web server used by keep_alive() so external ping services
# (e.g. UptimeRobot) can hit an HTTP endpoint and keep the bot process alive.
flask>=3.0.0

# ── System dependency (install separately) ────────────────────────────────────
# FFmpeg is required for audio processing.
# Linux:  sudo apt install ffmpeg
# macOS:  brew install ffmpeg
# Replit: already installed as a system dependency

# PO-Token provider plugin — cài để TẠO cấu trúc thư mục yt_dlp_plugins/extractor/.
# bot.py sẽ tự GHI ĐÈ 3 file bên trong bằng bản khớp version với server Rust
# (jim60105/bgutil-ytdlp-pot-provider-rs) — tránh lỗi "plugin/server version mismatch".
bgutil-ytdlp-pot-provider
